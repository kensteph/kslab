<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" type="text/css" href="/assets/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/assets/css/font-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="/assets/css/style.css">
    <style>

    </style>
</head>

<body>

    <div class="row-header">
        <div class="logo">
            <img style="margin-left: 10px;" src="data:image/jpeg;base64,<%=LOGO%>" alt="img" width="150" height="150">
        </div>
        <div class="header-form">
            <p>
            <h3> <%=line1%> </h3>
            <h3><%=line2%></h3>
            <% if(line3 !=""){ %>
            <%=line3%><br>
            <% } %>
            <% if(line4 !=""){ %>
            <%=line4%><br>
            <% } %>
            <% if(line5 !=""){ %>
            <%=line5%><br>
            <% } %>
            <% if(line6 !=""){ %>
            <%=line6%><br><br>
            <% } %>
            </p>
        </div>
    </div>
    <hr>
    <div class="row-header">
        <div class="title-report"><%=pageTitle%></div>
    </div>
    <div class="row-report" style="margin-right: 70px;">
        <div class="col-md-12">
            <div class="table-responsive">
                <table class="table table-striped custom-table datatable mb-0">
                    <thead>
                        <tr>
                            <th class="report">Date transaction</th>
                            <th class="report">Numéro Lot</th>
                            <th class="report">Matériau</th>
                            <th class="report">Quantité</th>
                            <th class="report">Commentaire</th>
                            <th class="report">Acteur</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% data.forEach((stock) => {
                            let fa = "";
                            let color = "color : red;";
                            let title = "";
                            if(stock.transaction == "add"){
                                fa = "fa fa-arrow-up";
                                title = "Augmentation de stock";
                                color = "color : green;";
                            }else if(stock.transaction == "substract"){
                                fa = "fa fa-arrow-down";
                                title = "Diminution de stock";
                            }else{
                                fa = "fa fa-arrow-down ";
                                title = "Stock endommagé";
                            }
                        %>
                        <tr>
                            <td class="report"><%=stock.date_record%></td>
                            <td class="report"><a href=""><%=stock.lot%></a></td>
                            <td class="report"><%=stock.nom_materiau%></td>
                            <td class="report"><%=stock.qte%></td>
                            <td class="report"><%=stock.commentaire%></td>
                            <td class="report"><%=stock.acteur%></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</body>

</html>